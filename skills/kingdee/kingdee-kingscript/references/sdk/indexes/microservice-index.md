# SDK Microservice Index

## 适用问题
- 某个能力是否属于微服务入口。
- 需要先按微服务定位，再追到模块、包或类。

## 检索顺序
1. 先看 `module-index.md` 缩小产品域。
2. 再看 `package-index.md` 和 `../microservices/index.md`。
3. 仍未命中时，回到当前项目依赖、声明文件或官方资料确认。
