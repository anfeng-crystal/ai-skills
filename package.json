{
  "name": "active-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "deps:install": "node scripts/npm-deps.mjs install",
    "deps:check": "node scripts/npm-deps.mjs check",
    "doctor": "node scripts/doctor.mjs --json"
  },
  "dependencies": {
    "@playwright/cli": "0.1.13"
  }
}
