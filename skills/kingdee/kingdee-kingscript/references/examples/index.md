# Kingscript Examples

## 使用规则
- 先按插件类型找最接近的示例。
- 已知事件名时，优先进入事件拆分目录。
- 插件示例入口：`plugins/index.md`
- 示例与 SDK 知识卡用于定位写法、事件、类型和 API 候选；生成或修改前按 `../sdk/strategy.md` 用匹配目标版本的声明/SDK 确认，不以示例版本提升用户目标。
- 同一场景只选一份主示例，不混用多份互相冲突的写法。
