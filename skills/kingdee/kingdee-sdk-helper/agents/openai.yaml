interface:
  display_name: "Kingdee SDK Helper"
  short_description: "苍穹 SDK 类、精确签名、Javadoc 与来源版本核验"
  default_prompt: "使用 $kingdee-sdk-helper 查询苍穹 SDK 类或方法，按核心规则分别给出来源版本、目标版本及兼容依据；索引命中仅作索引事实，生成项目示例前确认所用 API 适用于目标。"

policy:
  allow_implicit_invocation: true
