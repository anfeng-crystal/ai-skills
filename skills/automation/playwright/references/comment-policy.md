# Playwright 代码注释规范

- 默认使用 CLI 命令，不因注释策略主动生成测试文件。
- 用户明确要求 test/spec/helper 代码时，必须为测试意图、选择器假设、异步等待、fixture 副作用、数据清理和失败边界写稳定注释。
- 注释说明为什么等待、为什么选择该定位方式、清理动作保护什么状态；不逐行解释 `click`、`fill`、`expect` 等显而易见操作。
- 如果代码依赖固定账号、测试数据、权限、环境变量或网络状态，注释必须说明这些前置条件。
- 不把调试过程、录制过程、执行流水或一次性排查记录写进 spec 注释。
