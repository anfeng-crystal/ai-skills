interface:
  display_name: "Kingdee Security Review"
  short_description: "苍穹安全审计、社区发现核验、生产只读与受控 POC"
  default_prompt: "使用 $kingdee-security-review 先按证据来源选择 audit、audit-readonly、verify 或 redteam-lite；生产只读按完整契约执行，主动 POC 必须通过目标 scope 门禁。"

policy:
  allow_implicit_invocation: true
